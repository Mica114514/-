# 健身房实时数量检测浮窗 —— 从抓包到成品

一个「始终置顶的桌面小窗口」，每 60 秒自动获取某微信小程序的**场内实时人数**并显示。

> 学习目标：理解「微信小程序 → 抓包分析 → 找到数据接口 → 写程序定时拉数据 → 做成小窗」的完整链路。

---

## 一、成果是什么

| 文件 | 作用 |
|------|------|
| `live_count.py` | 主程序：定时请求接口 + 悬浮窗显示 |
| `健身房实时数量检测浮窗.bat` | 双击启动（用系统 `pyw` 启动器，无黑窗口） |

双击 `.bat` 后，桌面出现一个无边框、始终置顶的小窗，显示「场内实时人数」，60 秒刷新一次；可拖动、双击手动刷新、右键退出。

---

## 二、核心原理（先建立三个概念）

### 1. 微信小程序本质是「网页 + 一个受限的运行环境」

小程序界面是网页技术渲染的，但它发起的**网络请求就是普通的 HTTPS 请求**，打到小程序开发者自己的后端服务器（本例是 `app.fitoneapp.com`）。

> 所以只要知道「它请求了哪个 URL、用什么方式、带什么参数」，我们就能用任何语言（Python 等）**模拟同样的请求**，拿到同样的数据。

### 2. 抓包 = 中间人代理 + 解密 HTTPS

HTTPS 是加密的，正常情况下看不到明文。抓包工具（Fiddler）的原理是**中间人（MITM）**：

```
[微信小程序]  ←—— 加密 ——→  [Fiddler 代理]  ←—— 加密 ——→  [后端服务器]
                                 ↑
                           在这里解密、看到明文
                           再重新加密转发出去
```

因为小程序信任了 Fiddler 的根证书，所以它把 Fiddler 当成"服务器"来加密通信；Fiddler 再以"客户端"身份去连真正的服务器。于是请求/响应的明文对 Fiddler 完全可见。

### 3. 轮询（Polling）= 定时重复请求

"实时人数"其实是**每隔几秒请求一次接口，拿到最新值**。我们的程序也照做：每 60 秒请求一次，把返回的数字刷新到窗口上。

---

## 三、完整流程回顾（我们实际做了什么）

```
1. 抓包 → 2. 定位接口 → 3. 分析请求 → 4. 验证 → 5. 写程序
```

### 第 1 步：抓包

- 用 **Fiddler Classic**（Windows 免费抓包工具），开启 HTTPS 解密并信任它的证书。
- 从**电脑版微信**打开小程序，在 Fiddler 里按**进程 `wechatappex`** 过滤，只留下小程序发出的请求。
- 排除干扰：`config.office.com`（Windows/Office 噪音）、`getCoachListForModule`（教练列表，不是人数）。

### 第 2 步：定位接口

根据接口名判断意图，锁定这一条：

```
POST https://app.fitoneapp.com/appV3/3985/mg/admissionRecord/getMobileTodayCheckInCount
```

- `getMobileTodayCheckInCount` 字面意思 = 获取今日入场/签到人数。
- 它每隔几秒出现一次（轮询），且返回体很小（277 字节）——正是"一个数字"的量级。

### 第 3 步：分析请求（关键）

从 Fiddler 的 Request/Response 两栏读出的信息：

**请求（我们发出的）：**

| 部分 | 内容 | 说明 |
|------|------|------|
| 请求行 | `POST /appV3/.../getMobileTodayCheckInCount HTTP/1.1` | 用 **POST** 方法 |
| Content-Type | `application/x-www-form-urlencoded;charset=UTF-8` | body 是表单格式 |
| Content-Length | `2` | body 只有 2 字节 → 就是 `{}` |
| Referer | `https://servicewechat.com/wxfce3b2791d81afc6/...` | 标识来源小程序（appid） |
| User-Agent | `... MicroMessenger ... MiniProgramEnv/Windows ...` | 标识是微信小程序环境 |
| **鉴权头** | **没有 Authorization / Cookie / token / sign** | **接口是开放的！** |

**响应（服务器返回的）：**

```json
{
    "cn": 0,
    "code": "K/Magic/Model/0",
    "message": "获取成功",
    "data": {
        "insideMemberCount": 80,        // ← 场内实时人数，就是它！
        "isMember": false,
        "wssConfig": { "app_id": "...", "topic": "welcome3985", ... }
    }
}
```

- 人数藏在 `data.insideMemberCount` 里（字段路径：`data.insideMemberCount`）。
- 顺带发现 `wssConfig`：说明小程序真正"秒级实时"靠 **WebSocket 推送**；但对我们"60 秒瞄一眼"的需求，直接轮询 HTTP 接口就够。

### 第 4 步：验证（确认没搞错）

用 `curl` 和 Python 从**外面**直接请求这个接口，都成功返回 `insideMemberCount = 80`，证明：

- ✅ 接口无需登录、无需 token，长期运行不会失效
- ✅ 字段路径正确

### 第 5 步：写程序

把上面的请求用 Python 重放，套上一个 tkinter 悬浮窗，完成。

---

## 四、代码详解（伪代码 + 讲解）

### 整体逻辑（伪代码）

```
定义配置：URL、请求头、请求体、字段路径、刷新间隔

函数 获取人数():
    构造 POST 请求(URL, 请求头, 请求体)
    发送请求，拿到响应
    解析 JSON，按字段路径取出数字
    返回数字字符串

类 悬浮窗:
    初始化：无边框、置顶、绑定拖动/右键/双击事件
    启动后立即 获取人数() 一次，显示
    每 刷新间隔 秒，重复 获取人数() 并更新显示
    失败则显示"获取失败"，继续下一轮

主程序：
    创建 悬浮窗 并进入事件循环
```

### 1. 配置区

```python
URL = "https://app.fitoneapp.com/appV3/3985/mg/admissionRecord/getMobileTodayCheckInCount"
HEADERS = {
    "Content-Type": "application/x-www-form-urlencoded;charset=UTF-8",
    "Referer": "https://servicewechat.com/wxfce3b2791d81afc6/2/page-frame.html",
    "xweb_xhr": "1",
    "User-Agent": "Mozilla/5.0 ... MicroMessenger ... MiniProgramEnv/Windows ...",
}
BODY = "{}"                            # 请求体：空对象
FIELD_PATH = "data.insideMemberCount"  # 人数在响应 JSON 里的路径
INTERVAL = 60                          # 刷新间隔（秒）
```

> 这些值全部来自抓包结果，照抄即可。改 `INTERVAL` 就能调刷新频率。

### 2. 按字段路径取值 `get_path`

响应 JSON 可能有多层嵌套，用 `.` 分隔路径来定位：

```python
def get_path(obj, path):
    for key in path.split("."):   # "data.insideMemberCount" → ["data","insideMemberCount"]
        if isinstance(obj, list):
            obj = obj[int(key)]    # 遇到数组用下标，如 "list.0"
        else:
            obj = obj[key]         # 遇到字典用键名
    return obj
```

伪代码：
```
把 "a.b.c" 按 "." 切成 [a, b, c]
从最外层开始，一层一层往下取：
    a → 字典取值，得到 obj1
    b → 继续取值，得到 obj2
    c → 继续取值，得到最终结果
```

### 3. 发送请求 `fetch_count`

```python
import urllib.request, json

def fetch_count():
    req = urllib.request.Request(
        URL,
        data=BODY.encode("utf-8"),   # POST 请求体，转成字节
        headers=HEADERS,
        method="POST",
    )
    with urllib.request.urlopen(req, timeout=10) as resp:
        data = json.loads(resp.read().decode("utf-8"))  # 读取并解析 JSON
    return str(get_path(data, FIELD_PATH))              # 取出人数
```

伪代码：
```
用 POST 方式，带上请求头 + 请求体，请求 URL
收到响应后，把字节转成字符串，再解析成 JSON（字典）
从 JSON 里按路径取出人数，转成字符串返回
```

> 这里用 Python 标准库 `urllib`，无需额外安装第三方库。对比我们最初用 `curl` 验证，本质是同一件事：**构造同样的 HTTP 请求**。

### 4. 悬浮窗 + 定时刷新

```python
import tkinter as tk, threading

class FloatingWindow:
    def __init__(self):
        self.root = tk.Tk()
        self.root.overrideredirect(True)       # 去掉系统标题栏
        self.root.attributes("-topmost", True) # 始终置顶
        # ... 创建"人数"标签、"更新时间"标签 ...
        # ... 绑定：左键按下拖动、双击刷新、右键菜单退出 ...
        self.refresh()                         # 启动后立即拉一次

    def refresh(self):
        def work():                            # 网络请求放子线程，避免卡界面
            try:
                val = fetch_count()
                self.root.after(0, lambda: self.show_ok(val))   # 回主线程更新
            except Exception as ex:
                self.root.after(0, lambda: self.show_err(ex))
        threading.Thread(target=work, daemon=True).start()
        self.root.after(INTERVAL * 1000, self.refresh)  # 60 秒后再来一次
```

伪代码：
```
启动时先拉一次人数并显示
之后每 60 秒：
    开一个后台线程去请求（不阻塞界面）
    拿到数字 → 更新窗口上的数字 + 更新时间
    拿不到 → 显示"获取失败"，但继续下一轮
```

**两个要点：**

1. **为什么用子线程？** 网络请求要花时间（几百毫秒），如果放在主线程，界面会"卡住不动"。放子线程，界面始终流畅。
2. **`self.root.after(0, ...)` 是干嘛的？** tkinter 规定界面更新只能在主线程做。子线程拿到结果后，用 `after(0, ...)` 把"更新界面"这件事**丢回主线程执行**，避免线程冲突。

---

## 五、关键技术点小结

| 概念 | 一句话解释 |
|------|-----------|
| HTTPS 抓包 | 中间人代理 + 信任根证书，解密看到明文 |
| HTTP 请求三要素 | 方法(GET/POST) + 请求头(Headers) + 请求体(Body) |
| JSON | 键值对嵌套的数据格式，用路径定位字段 |
| 轮询 Polling | 定时重复请求，模拟"实时" |
| 置顶窗口 | tkinter 的 `-topmost` + `overrideredirect` |
| 多线程 | 网络请求放后台，界面不卡 |
| WebSocket | 服务器主动推送，真正的"秒级实时"（进阶方向） |

---

## 六、如何扩展学习

1. **改刷新频率**：改 `INTERVAL`（如 30 秒）。
2. **秒级实时**：响应里有 `wssConfig`（WebSocket 配置），可研究用 WebSocket 订阅人数变化，一变就更新，不用等 60 秒。
3. **开机自启**：把 `健身房实时数量检测浮窗.bat` 的快捷方式放进 `Win+R → shell:startup` 文件夹。
4. **换样式**：改 tkinter 标签的颜色、字体、透明度。
5. **换成 requests 库**：`import requests` 后 `requests.post(url, headers=..., data=...)` 更简洁（需 `pip install requests`）。
6. **抓别的数据**：用同样的抓包方法，找到温度/湿度字段（响应里其实也有 `temperatureData`），一起显示出来。

---

## 七、常见问题

- **双击 .bat 没反应**：`.bat` 内容含中文文件名会被 cmd 用 GBK 误读。已改成英文名 `live_count.py` + 纯英文 bat 内容解决。
- **显示"获取失败"**：多为网络问题或接口临时变更；打开终端跑 `python live_count.py` 看具体报错。
- **想停止**：右键小窗 → 退出。
